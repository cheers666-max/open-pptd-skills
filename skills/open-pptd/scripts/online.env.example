# Inject these variables through your host or private shell environment.
# This example is not loaded automatically. Never commit real credentials.
# Choose OBS when reusing an existing OBS config. OBS does not use PPT_API_KEY.
# PPT_UPLOAD_BACKEND=obs
# OBS_CONFIG=/absolute/path/to/protected-obs.yaml
# OBS_PROFILE=zhengzhou
# OBS_PREFIX=open-pptd/your-unique-release
# Without OBS_CONFIG or an explicit backend, the attachment service is used:
PPT_API_KEY=
PPT_API_BASE=https://aigw.aijjt.com
# Optional complete override (multipart file + response data.url):
S3_API_URL=
